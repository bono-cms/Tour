<?php

use Krystal\Form\Element;
defined('KRYSTAL') or die;

?>

<form data-group="date" autocomplete="off">
	<div class="well">
        <?php echo Element::hidden('date[id]', $date->getId()); ?>
        <?php echo Element::hidden('date[tour_id]', $date->getTourId()); ?>

        <div class="form-group">
			<label class="col-lg-2 control-label"><?php $this->show('Start date'); ?></label>
			<div class="col-lg-10">
				<div class="input-group">
                    <?php echo Element::text('date[start]', $date->getStart(), array('class' => 'form-control', 'data-plugin' => 'datepicker', 'placeholder' => $this->translate('Start date'))); ?>
					<span class="input-group-addon"><i class="glyphicon glyphicon-calendar"></i></span>
				</div>
			</div>
		</div>

		<div class="form-group">
			<label class="col-lg-2 control-label"><?php $this->show('End date'); ?></label>
			<div class="col-lg-10">
				<div class="input-group">
                    <?php echo Element::text('date[end]', $date->getEnd(), array('class' => 'form-control', 'data-plugin' => 'datepicker', 'placeholder' => $this->translate('End date'))); ?>
					<span class="input-group-addon"><i class="glyphicon glyphicon-calendar"></i></span>
				</div>
			</div>
		</div>
	</div>

    <?php $this->loadPartial('actions', array(
        'new' => !$date->getId(),
        'add' => 'Tour:Admin:TourDate@addAction',
        'edit' => 'Tour:Admin:TourDate@editAction',
        'save' => 'Tour:Admin:TourDate@saveAction',
        'cancel' => 'Tour:Admin:Tour@editAction'
    )); ?>

</form>
