<?php

use Krystal\Grid\Grid;
use Krystal\Form\Element;
use Cms\View\Icon;

defined('KRYSTAL') or die;

?>

<div class="btn-toolbar">
	<div class="btn-group">
	  <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown">
		<i class="glyphicon glyphicon-list"></i> <?php $this->show('Menu'); ?> <?php if ($newReviews > 0): ?>(<?= $newReviews; ?>)<?php endif; ?> <span class="caret"></span>
	  </button>

      <ul class="dropdown-menu">
		 <li><a href="<?php echo $this->url('Tour:Admin:Tour@addAction'); ?>"><i class="glyphicon glyphicon-file"></i> <?php $this->show('Add a tour'); ?></a></li>
		 <li><a href="<?php echo $this->url('Tour:Admin:Category@addAction'); ?>"><i class="glyphicon glyphicon-folder-open"></i> <?php $this->show('Add category'); ?></a></li>
		 <li><a href="<?php echo $this->url('Tour:Admin:Booking@indexAction', null); ?>"><i class="glyphicon glyphicon-th-list"></i> <?php $this->show('Bookings'); ?></a></li>
		 <li><a href="<?php echo $this->url('Tour:Admin:TourDestination@indexAction'); ?>"><i class="glyphicon glyphicon-resize-small"></i> <?php $this->show('Tour destinations'); ?></a></li>
		 <li>
            <?php if ($newReviews == 0): ?>
            <a href="<?php echo $this->url('Tour:Admin:TourReview@indexAction', null); ?>"><i class="glyphicon glyphicon-comment"></i> <?php $this->show('Reviews'); ?></a>
            <?php else: ?>
            <a href="<?php echo $this->url('Tour:Admin:TourReview@indexAction', null); ?>"><span class="text-danger"><i class="glyphicon glyphicon-comment"></i> <?php $this->show('Reviews'); ?> (<?= $newReviews; ?>)</span></a>
            <?php endif; ?>
         </li>
         <li class="divider"></li>
         <li class="dropdown-header"><?php $this->show('Actions'); ?></li>

         <?php if (!empty($tours)): ?>
         <li><a href="#" data-button="remove-selected" data-url="<?php echo $this->url('Tour:Admin:Tour@deleteAction', null); ?>"><i class="glyphicon glyphicon-remove"></i> <?php $this->show('Remove selected'); ?></a></li>
         <?php else: ?>
         <li class="disabled"><a><i class="glyphicon glyphicon-remove"></i> <?php $this->show('Remove selected'); ?></a></li>         
         <?php endif; ?>
	  </ul>
	</div>
</div>

<div class="row">
    <div class="col-lg-7">
        <?php $this->loadPartial('items-per-page', array('caption' => 'Tours')); ?>

        <form>
            <?= Grid::render($tours, array(
                // A column to be considered as a primary key
                'pk' => 'id',
                'batch' => true,
                'tableClass' => 'table table-hover table-bordered table-striped table-condensed',
                'columns' => array(
                    array(
                        'column' => 'id',
                        'label' => '#'
                    ),

                    array(
                        'column' => 'name',
                    ),

                    array(
                        'column' => 'adults',
                        'label' => 'Number of adults'
                    ),

                    array(
                        'column' => 'order',
                    ),

                    array(
                        'column' => 'published',
                        'translateable' => true,
                        'value' => function($row){
                            return $row['published'] ? 'Yes' : 'No';
                        }
                    )
                ),

                'actions' => array(
                    'view' => function($row){
                        return Icon::view($row->getUrl(), $this->translate('View this tour on site'));
                    },

                    'edit' => function($row){
                        return Icon::edit(
                            $this->url('Tour:Admin:Tour@editAction', $row['id']),
                            $this->translate('Edit this tour')
                        );
                    },

                    'remove' => function($row){
                        return Icon::remove(
                            $this->url('Tour:Admin:Tour@deleteAction', $row['id']),
                            $this->translate('Remove this tour')
                        );
                    }
                )

            ), $this->translator); ?>

            <?php if (empty($tours)): ?>
            <div class="center-block">
                <h2 class="text-center text-muted"><i class="glyphicon glyphicon-remove"></i> <?php $this->show('No tours'); ?></h2>
            </div>
            <?php endif; ?>
        </form>
    </div>

    <div class="col-lg-5">
        <h2><i class="glyphicon glyphicon-folder-open"></i> <?php $this->show('Categories'); ?> <small>(<?php echo count($categories); ?>)</small></h2>

        <?= Grid::render($categories, array(
            // A column to be considered as a primary key
            'pk' => 'id',
            'batch' => false,
            'tableClass' => 'table table-hover table-bordered table-striped table-condensed',
            'columns' => array(
                array(
                    'column' => 'id',
                    'label' => '#'
                ),

                array(
                    'column' => 'name',
                ),
            ),

            'actions' => array(
                'view' => function($row){
                    return Icon::view($row->getUrl(), $this->translate('View this category on site'));
                },

                'edit' => function($row){
                    return Icon::edit($this->url('Tour:Admin:Category@editAction', $row['id']), $this->translate('Edit this category'));
                },

                'remove' => function($row){
                    return Icon::remove($this->url('Tour:Admin:Category@deleteAction', $row['id']), $this->translate('Remove this category'));
                }
            )

        ), $this->translator); ?>

        <?php if (empty($categories)): ?>
        <div class="center-block">
            <h2 class="text-center text-muted"><i class="glyphicon glyphicon-remove"></i> <?php $this->show('No categories'); ?></h2>
        </div>
        <?php endif; ?>
    </div>
</div>