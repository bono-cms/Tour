<?php

use Krystal\Grid\Grid;
use Krystal\Form\Element;

?>

<div class="btn-toolbar">
	<div class="btn-group">
	  <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown">
		<i class="glyphicon glyphicon-list"></i> <?php $this->show('Menu'); ?> <span class="caret"></span>
	  </button>

      <ul class="dropdown-menu">
		 <li><a href="<?php echo $this->url('Tour:Admin:Tour@addAction'); ?>"><i class="glyphicon glyphicon-file"></i> <?php $this->show('Add a tour'); ?></a></li>
		 <li><a href="<?php echo $this->url('Tour:Admin:Category@addAction'); ?>"><i class="glyphicon glyphicon-folder-open"></i> <?php $this->show('Add category'); ?></a></li>
		 <li><a href="<?php echo $this->url('Tour:Admin:Booking@indexAction'); ?>"><i class="glyphicon glyphicon-th-list"></i> <?php $this->show('Bookings'); ?></a></li>
         <li class="divider"></li>
         <li class="dropdown-header"><?php $this->show('Actions'); ?></li>

         <?php if (!empty($tours)): ?>
         <li><a href="#" data-button="remove-selected" data-url="<?php echo $this->url('Tour:Admin:Tour@deleteAction', null); ?>"><i class="glyphicon glyphicon-remove"></i> <?php $this->show('Remove selected'); ?></a></li>
         <?php else: ?>
         <li class="disabled"><a><i class="glyphicon glyphicon-remove"></i> <?php $this->show('Remove selected'); ?></a></li>         
         <?php endif; ?>
	  </ul>
	</div>
</div>

<div class="row">
    <div class="col-lg-6">
        <?php $this->loadPartial('items-per-page', array('caption' => 'Tours')); ?>

        <form>
            <?= Grid::render($tours, array(
                // A column to be considered as a primary key
                'pk' => 'id',
                'batch' => true,
                'tableClass' => 'table table-hover table-bordered table-striped table-condensed',
                'columns' => array(
                    array(
                        'column' => 'id',
                        'label' => '#'
                    ),

                    array(
                        'column' => 'name',
                    ),

                    array(
                        'column' => 'adults',
                        'label' => 'Number of adults'
                    ),

                    array(
                        'column' => 'order',
                    ),

                    array(
                        'column' => 'published',
                        'translateable' => true,
                        'value' => function($row){
                            return $row['published'] ? 'Yes' : 'No';
                        }
                    )
                ),
                
                'actions' => array(
                    'view' => function($row){
                        return Element::icon('glyphicon glyphicon-search', $row->getUrl(), array('target' => '_blank', 'data-toggle' => 'tooltip', 'data-placement' => 'left', 'data-original-title' => $this->translate('View this tour on site')));
                    },

                    'edit' => function($row){
                        return Element::icon('glyphicon glyphicon-pencil', $this->url('Tour:Admin:Tour@editAction', $row['id']), array('data-toggle' => 'tooltip', 'data-placement' => 'left', 'data-original-title' => $this->translate('Edit this tour'), 'data-button' => 'edit'));
                    },

                    'remove' => function($row){
                        return Element::icon('glyphicon glyphicon-remove', '#', array('data-toggle' => 'tooltip', 'data-placement' => 'left', 'data-original-title' => $this->translate('Remove this tour'), 'data-button' => 'delete', 'data-url' => $this->url('Tour:Admin:Tour@deleteAction', $row['id'])));
                    }
                )
            ), $this->translator); ?>
        </form>
    </div>

    <div class="col-lg-6">
        <h2><?php $this->show('Categories'); ?> <small>(<?php echo count($categories); ?>)</small></h2>

        <?= Grid::render($categories, array(
            // A column to be considered as a primary key
            'pk' => 'id',
            'batch' => false,
            'tableClass' => 'table table-hover table-bordered table-striped table-condensed',
            'columns' => array(
                array(
                    'column' => 'id',
                    'label' => '#'
                ),

                array(
                    'column' => 'name',
                ),
            ),

            'actions' => array(
                'view' => function($row){
                    return Element::icon('glyphicon glyphicon-search', $row->getUrl(), array('target' => '_blank', 'data-toggle' => 'tooltip', 'data-placement' => 'left', 'data-original-title' => $this->translate('View this category on site')));
                },

                'edit' => function($row){
                    return Element::icon('glyphicon glyphicon-pencil', $this->url('Tour:Admin:Category@editAction', $row['id']), array('data-toggle' => 'tooltip', 'data-placement' => 'left', 'data-original-title' => $this->translate('Edit this category'), 'data-button' => 'edit'));
                },

                'remove' => function($row){
                    return Element::icon('glyphicon glyphicon-remove', '#', array('data-toggle' => 'tooltip', 'data-placement' => 'left', 'data-original-title' => $this->translate('Remove this category'), 'data-button' => 'delete', 'data-url' => $this->url('Tour:Admin:Category@deleteAction', $row['id'])));
                }
            )

        ), $this->translator); ?>

        <?php if (empty($categories)): ?>
        <div class="center-block">
            <h2 class="text-center text-muted"><i class="glyphicon glyphicon-remove"></i> <?php $this->show('No categories'); ?></h2>
        </div>
        <?php endif; ?>
    </div>
</div>